import { expect } from "chai";

import * as main from "./main";

describe("main", () => {
  it("should return true", async () => {
    expect(true).to.be.true; // We can'd directly do much here because this is an example that connects to external services
  });
});
